// lib/domain/services/derived_training_context.dart

/// Legacy stub for backward compatibility.
/// Migrate to training_v3 engines.
class DerivedTrainingContext {
  @Deprecated('Legacy. Migrar a training_v3 context.')
  DerivedTrainingContext({required Map<String, Object?> input}) {
    throw UnimplementedError(
      'DerivedTrainingContext es legacy. Migrar a training_v3.',
    );
  }
}

class GluteSpecializationContext {
  @Deprecated('Legacy. Migrar a training_v3 context.')
  GluteSpecializationContext({required Map<String, Object?> input}) {
    throw UnimplementedError(
      'GluteSpecializationContext es legacy. Migrar a training_v3.',
    );
  }
}
